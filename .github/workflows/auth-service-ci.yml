name: Auth Service CI

on:
  push:
    branches: [dev_ishanka]
    paths:
      - 'services/auth-service/**'
  pull_request:
    branches: [dev_ishanka]
    paths:
      - 'services/auth-service/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        working-directory: services/auth-service
        run: mvn clean compile -DskipTests

      - name: Run tests
        working-directory: services/auth-service
        run: mvn test
        continue-on-error: true

      - name: Package JAR
        working-directory: services/auth-service
        run: mvn package -DskipTests

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: auth-service-jar
          path: services/auth-service/target/*.jar
          retention-days: 7

  # Optional: Deploy job (uncomment when ready)
  # deploy:
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     
  #     - name: Log in to Azure
  #       uses: azure/login@v1
  #       with:
  #         creds: ${{ secrets.AZURE_CREDENTIALS }}
  #     
  #     - name: Build and Push Docker Image
  #       run: |
  #         az acr login --name ${{ secrets.ACR_NAME }}
  #         docker build -t ${{ secrets.ACR_NAME }}.azurecr.io/auth-service:${{ github.sha }} -f services/auth-service/Dockerfile .
  #         docker push ${{ secrets.ACR_NAME }}.azurecr.io/auth-service:${{ github.sha }}
  #     
  #     - name: Deploy to Azure Container Apps
  #       uses: azure/container-apps-deploy-action@v1
  #       with:
  #         resourceGroup: ${{ secrets.RESOURCE_GROUP }}
  #         containerAppName: auth-service
  #         imageToDeploy: ${{ secrets.ACR_NAME }}.azurecr.io/auth-service:${{ github.sha }}
